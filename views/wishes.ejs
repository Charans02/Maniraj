<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Wishes</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #ffe6f0 0%, #fff0f5 100%);
      background-attachment: fixed;
      font-family: 'Segoe UI', sans-serif;
    }
    .wish-card {
      background: rgba(255,255,255,0.95);
      border-radius: 16px;
      padding: 2rem;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      max-width: 700px;
      margin: auto;
    }
    .guest-message {
      background: #fff;
      border: 1px solid #eee;
      border-radius: 10px;
      padding: 1rem;
      margin-top: 1rem;
      text-align: left;
    }
    .btn-back {
      background-color: #ff4d6d;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 25px;
      font-weight: bold;
      text-decoration: none;
      transition: background-color 0.3s ease;
    }
    .btn-back:hover {
      background-color: #e03e5f;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="wish-card text-center">
      <h2 class="text-danger mb-3">With All My Love</h2>
      <p class="lead">
        Dear Mom and Dad,<br><br>
        Happy 26th Anniversary! Your love has been the guiding light of our family.<br>
        Thank you for showing us the meaning of commitment, joy, and togetherness.<br>
        Wishing you many more beautiful years ahead!
      </p>

      <!-- Form to submit wishes -->
      <form action="/wishes" method="POST" class="mt-4">
        <div class="mb-3 text-start">
          <label for="name" class="form-label">Your Name</label>
          <input type="text" class="form-control" id="name" name="name" required>
        </div>
        <div class="mb-3 text-start">
          <label for="message" class="form-label">Your Message</label>
          <textarea class="form-control" id="message" name="message" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-success">Submit Your Wish</button>
      </form>

      <!-- List of guest messages -->
      <% if (guestMessages.length > 0) { %>
        <h4 class="mt-5">Messages from Family & Friends:</h4>
        <% guestMessages.forEach(function(msg) { %>
          <div class="guest-message">
            <strong><%= msg.name %>:</strong><br>
            <%= msg.message %>
          </div>
        <% }); %>
      <% } %>

      <div class="mt-4">
        <a href="/" class="btn-back">Back to Home</a>
      </div>
    </div>
  </div>
</body>
</html>
